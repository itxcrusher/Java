package Procedural;

// ! Different tyes of operators in Java

public class Operators {
    // * Arithmetic Operators
    // ? + --> Addition
    // ? - --> Subtraction
    // ? * --> Multiplication
    // ? / --> Division
    // ? % --> Modulus
    // ? ++ --> Increment
    // ? -- --> Decrement

    // * Relational | Comparison Operators
    // ? == --> Equal to
    // ? != --> Not Equal to
    // ? > --> Greater than
    // ? < --> Less than
    // ? >= --> Greater than Equal to
    // ? <= --> Less than Equal to

    // * Logical Operators
    // ? && --> AND
    // ? || --> OR
    // ? ! --> NOT

    // * Assignment Operators
    // ?  --> Assign
    // ?  --> Add and Assign
    // ?  --> Subtract and Assign
    // ?  --> Multiply and Assign
    // ?  --> Divide and Assign
    // ?  --> Mod and Assign
}